# 🔒 EXEMPLE DE CONFIGURATION - NE PAS UTILISER EN PRODUCTION
# Copiez ce fichier en .env et remplissez vos valeurs personnelles
# CE FICHIER EST SÉCURISÉ POUR LE REPO PUBLIC

# 🔧 Configuration générale
NODE_ENV=development
PORT=3001

# 🔒 CORS Configuration (liste des origines autorisées)
# Format: domaine1,domaine2,domaine3
ALLOWED_ORIGINS=http://localhost:5000,http://127.0.0.1:5000

# 🔒 Protection contre SSRF (domaines autorisés pour les requêtes)
# Format: domaine1,domaine2 (les sous-domaines sont automatiquement autorisés avec .domaine)
# Exemple: .xtream-codes.com,.xtream.io
ALLOWED_DOMAINS=.xtream-codes.com,.xtream.io,.xtream.tv,.iptv.com

# 🔑 Clé API optionnelle (laisser vide pour désactiver)
# Si configurée, toutes les requêtes doivent inclure: X-API-Key: votre_clé
API_KEY=votre_clé_secrète_ici

# 🔧 URLs de services externes (exemples)
# DATABASE_URL=postgresql://user:pass@localhost:5432/db
# REDIS_URL=redis://localhost:6379

# 📧 Configuration email (optionnel)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=votre@email.com
# SMTP_PASS=votre_mot_de_passe

# 🚨 Notification de sécurité (optionnel)
# ADMIN_EMAIL=admin@votreapp.com
